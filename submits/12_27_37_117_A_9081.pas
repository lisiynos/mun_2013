program PROG532_E;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var k, i, n, j, d: Integer;
   s:array[12,4] of Integer;
begin
  { TODO -oUser -cConsole Main : Insert code here }
  reset(Input, 'erd.in');
  Rewrite(Output, 'erd.out');
  n := Random(20) - 2;
  e := Random(1000000000) - 1;
  for i := 1 to 12 do begin
    for j := 1 to 4 do begin
     if
    end;
  end;
end.
